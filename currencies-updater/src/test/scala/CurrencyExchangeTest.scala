import org.scalatest.funspec.AnyFunSpec
import org.scalatest.matchers.should.Matchers

object mock {

}

class CurrencyExchangeTest extends AnyFunSpec with Matchers {
  describe("") {
    // import mock._
    it("") {
      // val exchangeRateInterp = CurrencyExchangeRateDSL[IO]
      // val baseCurrency = Currency.getInstance("USD")
      // val map = exchangeRateInterp.load(baseCurrency).unsafeRunSync()
      // map.size shouldEqual 2
      // map.get(Currency.getInstance("PLN")) shouldEqual Some(1.89)
    }
  }
}
